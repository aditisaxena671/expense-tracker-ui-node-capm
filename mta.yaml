_schema-version: 3.3.0
ID: expense-tracker
description: A simple CAP project.
version: 1.0.0
modules:
  - name: expense-tracker-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: expense-tracker-db
      - name: expense-tracker-auth
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: expense-tracker-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: expense-tracker-db
    parameters:
      buildpack: nodejs_buildpack
  - name: expense-tracker-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: expense-tracker-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - aditiexpenseuiproject1.zip
          name: aditiexpenseuiproject1
          target-path: resources/
  - name: aditiexpenseuiproject1
    type: html5
    path: app/project1
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: expense-tracker-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: expense-tracker-auth
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        tenant-mode: dedicated
        xsappname: expense-tracker-${org}-${space}
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
  - name: expense-tracker-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: expense-tracker-html5-service
      service-plan: app-host
  - name: expense-tracker-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: expense-tracker-destination-service
      service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
